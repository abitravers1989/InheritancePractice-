using System;
using System.Collections.Generic;
using System.Text;

namespace AnimalConsoleApp.cs
{
    // if don't make it public won't be able to create a class in prgramme
    public class Shark : Animal
    {

        // don't write properities again here 

        public Shark(string name, int age, string status):base(age, 0, name, "shark", status)
        {}
           
       


    }

}
